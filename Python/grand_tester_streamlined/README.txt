example terminal input(when already in folder): python tester_main.py ./ 100
The 100 is the number of iterations the code will run. The presented results are averages + min&max

tester_main.py is contains the main, used for configuration of the testing + which BFs are being run. Also used to configure BFAR (simple array BF).

All other BFs are configured in their own file.